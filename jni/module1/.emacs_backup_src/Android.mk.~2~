LOCAL_PATH := $(call my-dir)
include $(CLEAR_VARS)

LOCAL_MODULE := glob
LOCAL_SRC_FILES := $(LOCAL_PATH)/glob.c
LOCAL_C_INCLUDES := $(LOCAL_PATH)/glob.h
#LOCAL_CFLAGS += -fPIE
#LOCAL_LDFLAGS += -fPIE -pie

include $(BUILD_SHARED_LIBRARIES)
